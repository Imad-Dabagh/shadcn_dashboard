import { Card } from './ui/card';
import { ScrollArea } from './ui/scroll-area';

const TodoList = () => {
  return (
    <div className="">
      Calendar
      {/* LIST */}
      <ScrollArea className="max-h-[400px] mt-4 overflow-y-auto">
        {/* List Items */}
        <Card>ddddddddddd</Card>
      </ScrollArea>
    </div>
  );
};

export default TodoList;
